The cluster admin builds the raw image(s) needed for the edge devices to be brought into the cluster
There are scripts/make commands for building for linux machines and specific raspberry pis

Copy amqp-config.sample.yaml to amqp-config.yaml and fill in the details of the Rabbitmq cluster that the agent should connect to