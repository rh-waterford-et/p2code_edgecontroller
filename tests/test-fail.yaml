apiVersion: v1
kind: Pod
metadata:
  name: invalid-bootc-edge-app
spec:
  containers:
  - image: quay.io/clwalsh/bootc/bootc-httpd-server:v1
    name: invalid-bootc-edge-app
    imagePullPolicy: Always  
  nodeSelector:
    kubernetes.io/role: agent
    type: virtual-kubelet
  tolerations:
  - key: virtual-kubelet.io/provider
    operator: Exists